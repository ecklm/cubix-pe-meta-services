---
name: Delete Student Zone

"on":
  workflow_dispatch:
    inputs:
      subdomain_name:
        description: Subdomain to delete under cubix.ecklm.com
        required: true
        type: string
      port_context:
        description: Port action run context
        required: false
        default: '{"runId":""}'
        type: string

permissions:
  actions: write
  contents: write
  pull-requests: write

jobs:
  create-pr:
    name: Create Student Zone PR
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Delete student zone entry
        env:
          SUBDOMAIN_NAME: ${{ inputs.subdomain_name }}
        run: |
          set -euo pipefail
          subdomain="${SUBDOMAIN_NAME,,}"
          subdomain="${subdomain//[[:space:]]/}"

          if [[ ! "${subdomain}" =~ ^[a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?$ ]]; then
            echo "subdomain_name must be a single DNS label using lowercase letters, numbers, or hyphens" >&2
            exit 1
          fi

          zone_path="students/${subdomain}.json"

          if [[ ! -f "${zone_path}" ]]; then
            echo "student zone does not exist: ${subdomain}" >&2
            exit 1
          fi

          rm "${zone_path}"

      - name: Terraform Format
        run: terraform fmt -recursive

      - name: Generate Port PR Token
        id: port-pr-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.PORT_PR_APP_ID }}
          private-key: ${{ secrets.PORT_PR_APP_PRIVATE_KEY }}

      - name: Create Pull Request
        id: cpr
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ steps.port-pr-token.outputs.token }}
          branch: port/delete-${{ inputs.subdomain_name }}
          delete-branch: true
          commit-message: "user: Delete ${{ inputs.subdomain_name }}"
          title: "user: Delete ${{ inputs.subdomain_name }}"
          body: |
            Port.io requested deletion for `${{ inputs.subdomain_name }}.cubix.ecklm.com`.

            This PR removes `students/${{ inputs.subdomain_name }}.json`.

      - name: Enable Auto-Merge PR
        env:
          GH_TOKEN: ${{ steps.port-pr-token.outputs.token }}
          PR_URL: ${{ steps.cpr.outputs.pull-request-url }}
        run: |
          set -euo pipefail
          gh pr merge "$PR_URL" --auto --merge

          echo "Waiting for PR to be merged..."
          while [[ "$(gh pr view "$PR_URL" --json state --jq .state)" != "MERGED" ]]; do
            if [[ "$(gh pr view "$PR_URL" --json state --jq .state)" == "CLOSED" ]]; then
              echo "PR was closed without merging."
              exit 1
            fi
            sleep 10
          done
          echo "PR successfully merged."

      - name: Report Final Status to Port
        if: ${{ always() && fromJson(inputs.port_context).runId != '' }}
        uses: port-labs/port-github-action@v1
        with:
          clientId: ${{ secrets.PORT_CLIENT_ID }}
          clientSecret: ${{ secrets.PORT_CLIENT_SECRET }}
          operation: PATCH_RUN
          runId: ${{ fromJson(inputs.port_context).runId }}
          status: ${{ job.status == 'success' && 'SUCCESS' || 'FAILURE' }}
          logMessage: "GitHub workflow completed with status: ${{ job.status }}"
          link: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
